# Core (everything except the demo recorder).
numpy>=1.26
torch>=2.2
opencv-python>=4.9

# Tests.
pytest>=8.0
pytest-cov>=5.0

# Optional. The demo recorder needs ffmpeg on PATH. Narration uses the local
# Windows voice (System.Speech) by default and needs no package at all;
# edge-tts is only a fallback for machines without it.
edge-tts>=6.1
